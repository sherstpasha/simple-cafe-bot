## 📌 **Simple-Cafe-Bot (Inline-кнопки)**

### **Telegram-бот для учета заказов в кафе**

---

## 🧩 **Описание**

Бот предназначен для учёта заказов напитков и десертов. Он:

* Добавляет заказы по шагам
* Удаляет записи (в т.ч. «Очистить за сегодня»)
* Формирует Excel-отчёты
* Ведёт журнал всех действий (в т.ч. удалённых)
  Работает с несколькими пользователями, использует SQLite и pandas.

---

## 💾 **База данных (SQLite)**

### Таблица `orders`

| Поле          | Тип                 | Описание                           |
| ------------- | ------------------- | ---------------------------------- |
| id            | INTEGER PRIMARY KEY | Уникальный ID записи               |
| date          | TEXT                | Дата и время создания (ISO-формат) |
| payment\_type | TEXT                | "Наличный" или "Безналичный"       |
| item\_name    | TEXT                | Название напитка или десерта       |
| username      | TEXT                | Telegram username                  |
| user\_id      | INTEGER             | Telegram user ID                   |

---

### Таблица `actions_log`

| Поле          | Тип     | Описание                                      |
| ------------- | ------- | --------------------------------------------- |
| id            | INTEGER | Уникальный ID действия                        |
| timestamp     | TEXT    | Дата и время действия                         |
| action\_type  | TEXT    | `добавление`, `удаление`, `очистка_сегодня`   |
| payment\_type | TEXT    | Тип оплаты (если применимо)                   |
| item\_name    | TEXT    | Название напитка или десерта (если применимо) |
| user\_id      | INTEGER | Telegram user ID                              |
| username      | TEXT    | Telegram username                             |

---

### 📋 **Поведение сообщений**

> 🔹 **Все сообщения пользователя удаляются** — на любом этапе взаимодействия с ботом:  
> – выбор действий  
> – ввод названия  
> – любые случайные сообщения  
>
> 🔹 В чате остаются только:  
> – актуальное сообщение бота с inline-кнопками  
> – сообщения с результатами (например, ✅ Запись добавлена)

📦 Это обеспечивает **чистый и понятный интерфейс**, без лишнего шума.

## 🧑‍💻 **Главное меню**

Главное меню вызывается по `/start` и после завершения операций. Отображается как **inline-кнопки**, встроенные в сообщение:

**Inline-кнопки:**

* 🔘 **Добавить**
* ❌ **Удалить**
* 📄 **Получить отчёт**

> ⚠️ Главное меню всегда создаётся **новым сообщением**, а старое очищается.
> Это сообщение сохраняется для последующего редактирования.

---

## ➕ **Добавление записи**

Работает в 3 шага через **inline-интерфейс** (FSM):

---

### **Шаг 1 — Выбор типа оплаты**

Сообщение:

> 🔹 *"Выберите тип оплаты:"*

**Inline-кнопки:**

* Наличный
* Безналичный
* Назад — возврат в главное меню

🧹 Предыдущее сообщение бота редактируется
🧹 Сообщение пользователя удаляется (если было)

---

### **Шаг 2 — Ввод названия напитка**

Бот отправляет:

> 🔹 *"Тип оплаты: Наличный, введите название напитка или десерта:"*

**Inline-кнопка внизу:** 🔙 Назад

Пользователь вводит название обычным сообщением, оно **автоматически удаляется**

---

### **Шаг 3 — Подтверждение добавления**

Бот спрашивает:

> 🔹 *Добавить «Наличный, Капучино»?*

**Inline-кнопки:**

* ✅ Добавить
* ❌ Отмена

---

### ✅ После подтверждения или отмены:

1. 🧹 Сообщение с кнопками (`Добавить / Отмена`) — **удаляется**
2. ✅ Если добавлено — появляется отдельное сообщение:

```
✅ Запись от 2025-06-14 13:35, Наличный, Флэт Уайт добавлена.
```

3. 🟢 Следом — новое сообщение с **главным меню (inline-кнопки)**

---

> ✅ Главное: интерфейс остаётся чистым — в чате остаются только подтверждённые действия и актуальное главное меню.

---

## ➖ **Удаление записи**

**(Всё реализовано на inline-кнопках)**

Показывается список из последних 5 заказов пользователя:

```
1. 2025-06-14 12:31 | Наличный | Капучино
2. ...
```

**Inline-кнопки:**

* 1️⃣ … 5️⃣ — удалить соответствующую запись
* ⏭ Далее — показать следующую пятёрку
* 🧹 Очистить за сегодня
* 🔙 Назад в меню

После удаления:

* Запись удаляется из `orders`
* Логируется в `actions_log`
* Сообщение обновляется

---

### 🔁 Подтверждение очистки за сегодня

Бот отправляет сообщение:

> 🔸 Очистить все записи за сегодня?

**Inline-кнопки:**

* ✅ Очистить
* ❌ Отмена

После подтверждения:

* Каждая запись удаляется и логируется
* Появляется сообщение:

```
✅ Очищено 3 записи за 2025-06-14
```

* Главное меню отправляется отдельно

---

## 📄 **Excel-отчёты**

Команда **📄 Получить отчёт** из меню вызывает отправку двух файлов:

### 1. `report.xlsx` — заказы

Содержит 4 листа:

1. Все заказы — `Дата | Тип оплаты | Название`
2. Только "Наличный"
3. Только "Безналичный"
4. Группировка по названию: `Название | Кол-во`

### 2. `log_report.xlsx` — лог действий

| Дата/время | Действие   | Тип оплаты | Название | user\_id | username |
| ---------- | ---------- | ---------- | -------- | -------- | -------- |
| 2025-06-14 | добавление | Наличный   | Латте    | 123456   | @user1   |

---

## 👥 **Работа с несколькими пользователями**

* Каждый видит и управляет **только своими** заказами
* Отчёты формируются по **всей базе**
* В `report.xlsx` **нет usernames**
* В `log_report.xlsx` usernames сохраняются

---

## 🛠️ **Технологии**

* Python 3.10+
* Aiogram 3.x
* SQLite (`sqlite3`)
* pandas + openpyxl (Excel)
* `python-dotenv`

---

## 🔄 **FSM-состояния**

* `awaiting_payment_type`
* `awaiting_item_name`
* `awaiting_add_confirmation`

---

## 📁 **Структура проекта**

```
project/
├── bot.py               # Основная логика (с FSM)
├── config.py            # Загрузка токена
├── db.py                # Работа с базой данных
├── reports.py           # Генерация Excel-файлов
├── .env                 # BOT_TOKEN
├── requirements.txt     # Зависимости
└── orders.db            # SQLite база
```

---

## 📦 Пример `.env`

```
BOT_TOKEN=your_telegram_bot_token_here
```

---

## ✅ Функции

| Функция               | Описание                                       |
| --------------------- | ---------------------------------------------- |
| 🔘 **Добавить**       | Пошаговая inline-форма, запись в БД и лог      |
| ❌ **Удалить**         | По 5 штук, кнопки 1–5, очистка за сегодня, лог |
| 📄 **Получить отчёт** | Отправка `report.xlsx` и `log_report.xlsx`     |

